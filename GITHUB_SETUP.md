# 🚀 Загрузка проекта на GitHub - Пошаговая инструкция

## ✅ Шаг 1: Финальная проверка безопасности

Перед загрузкой убедитесь, что нет секретов:

```bash
# Проверьте, что .env не попадет в коммит
git check-ignore .env

# Проверьте, что нет реальных API ключей в коде
grep -r "AIza" --exclude-dir=venv --exclude-dir=__pycache__ . || echo "OK"
```

## 📦 Шаг 2: Инициализация Git (если еще не сделано)

```bash
cd /Users/natali/Downloads/Практика/for_me

# Инициализируйте репозиторий
git init

# Проверьте статус
git status
```

## 📝 Шаг 3: Настройка Git (если первый раз)

```bash
# Установите ваше имя и email (если еще не установлено)
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

## ➕ Шаг 4: Добавление файлов

```bash
# Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# Проверьте, что добавилось (должны быть все файлы, кроме .env, venv/, __pycache__)
git status
```

**Важно:** Убедитесь, что `.env` НЕ в списке добавленных файлов!

## 💾 Шаг 5: Первый коммит

```bash
# Создайте первый коммит
git commit -m "Initial commit: FOR ME product compatibility system

- Multi-agent system with OrchestratorAgent
- Category-specific compatibility agents (Food, Cosmetics, Household)
- Scoring system with Safety, Sensitivity, Match scores
- Long-term and short-term memory
- FastAPI server with /chat and /chat/upload endpoints
- Unit tests (56 tests)
- Complete API documentation
- Image OCR support via Gemini Vision API"
```

## 🌐 Шаг 6: Создание репозитория на GitHub

1. Откройте [GitHub.com](https://github.com) и войдите в аккаунт
2. Нажмите **"+"** в правом верхнем углу → **"New repository"**
3. Заполните:
   - **Repository name:** `for-me` (или другое имя)
   - **Description:** `Multi-agent product compatibility system using Google ADK`
   - **Visibility:** Public или Private (на ваш выбор)
   - **НЕ** добавляйте README, .gitignore, license (у вас уже есть)
4. Нажмите **"Create repository"**

## 🔗 Шаг 7: Подключение к GitHub

GitHub покажет инструкции. Выполните:

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/for-me.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/for-me.git

# Проверьте, что remote добавлен
git remote -v
```

## 📤 Шаг 8: Загрузка кода на GitHub

```bash
# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код
git push -u origin main
```

Если GitHub запросит авторизацию:
- Для HTTPS: используйте Personal Access Token (не пароль)
- Для SSH: убедитесь, что SSH ключ добавлен в GitHub

## ✅ Шаг 9: Проверка

Откройте ваш репозиторий на GitHub и убедитесь:
- ✅ Все файлы загружены
- ✅ README.md отображается
- ✅ .env НЕ в репозитории
- ✅ venv/ НЕ в репозитории

## 🔒 Безопасность

После загрузки проверьте:
1. `.env` не должен быть виден в репозитории
2. Нет реальных API ключей в коде
3. `.env.example` есть в репозитории (это нормально)

## 📚 Дополнительно

### Если нужно обновить код позже:

```bash
# Добавьте изменения
git add .

# Создайте коммит
git commit -m "Описание изменений"

# Загрузите на GitHub
git push
```

### Если забыли добавить .env в .gitignore:

```bash
# Удалите .env из индекса (но оставьте локально)
git rm --cached .env

# Создайте коммит
git commit -m "Remove .env from repository"

# Загрузите
git push
```

---

**Готово!** 🎉 Ваш проект теперь на GitHub!

